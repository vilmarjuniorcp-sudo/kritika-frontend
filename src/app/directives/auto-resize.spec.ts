import { AutoResize } from './auto-resize';

describe('AutoResize', () => {
  it('should create an instance', () => {
    const directive = new AutoResize();
    expect(directive).toBeTruthy();
  });
});
